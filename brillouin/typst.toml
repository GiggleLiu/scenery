[package]
name = "brillouin"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["GiggleLiu"]
license = "MIT"
description = "Brillouin zones, high-symmetry k-paths and band-path panels for Typst"
repository = "https://github.com/GiggleLiu/scenery"
keywords = ["brillouin", "reciprocal lattice", "band structure", "k-path", "crystallography", "physics", "visualization"]
categories = ["visualization"]
compiler = "0.14.0"
exclude = ["tests/*"]
