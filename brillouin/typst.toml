[package]
name = "brillouin"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Jinguo Liu <@GiggleLiu>"]
license = "MIT"
description = "Draw Brillouin zones, k-paths, and band-path panels."
repository = "https://github.com/GiggleLiu/scenery"
homepage = "https://giggleliu.github.io/scenery/"
keywords = ["brillouin", "reciprocal lattice", "band structure", "k-path", "crystallography", "physics", "visualization"]
categories = ["visualization"]
compiler = "0.14.0"
exclude = ["/tests/*", "/examples/*", "/images/*", "/Makefile"]
