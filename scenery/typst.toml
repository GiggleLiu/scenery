[package]
name = "scenery"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["GiggleLiu"]
license = "MIT"
description = "2D/3D scene rendering for Typst: typed primitives, orthographic projection, depth-sorted CeTZ output"
repository = "https://github.com/GiggleLiu/scenery"
compiler = "0.14.0"
exclude = ["tests/*"]
